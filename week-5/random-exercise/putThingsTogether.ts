'use strict';
export { }

/* declare function require(path: string): any;
const fs = require('fs');

function deleteCharacter(): string {
    let content = fs.readFileSync('swearword.txt', 'utf-8');
    let contentBeArray: string[] = content.toString().toLowerCase().split(' ');
    let myDestroy: RegExp[] = [/\?/gi, /\./gi, /\,/gi];
    for (let i: number = 0; i < myDestroy.length; i++) {
        mystring = mystring.replace(myDestroy[i], "");
    }
    return mystring;
} */
